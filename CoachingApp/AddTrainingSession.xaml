<Window x:Class="CoachingApp.AddTrainingSession"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:CoachingApp"
        mc:Ignorable="d"
        Title="AddTrainingSession" Height="428" Width="538">
    <Grid>
        <TextBlock HorizontalAlignment="Center" Margin="0,38,0,0" TextWrapping="Wrap" Text="Add New Training Session" VerticalAlignment="Top" Width="258" Height="40" FontSize="20" FontWeight="Bold"/>
        
        <StackPanel Margin="20,83,20,0">

            <DatePicker x:Name="dpDate"
                Margin="5"/>

            <materialDesign:TimePicker x:Name="tpTime"
             Margin="5"
             Height="30"/>

            <ComboBox x:Name="cbType"
            Margin="5"
            Height="30"
            Text="Session Type :"/>
            
            <materialDesign:NumericUpDown x:Name="nudDuration"
            Margin="5"
            Height="30"/>

            <TextBox x:Name="txtNotes"
             Margin="5"
             Height="111"
             AcceptsReturn="True"
             TextWrapping="Wrap"
             Text="Coach Notes"/>

            <Button Content="Save"
            Width="100"
            Height="40"
            Margin="5"
            Click="BtnSave_Click"/>

        </StackPanel>
    </Grid>
</Window>
